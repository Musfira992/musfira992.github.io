import type { Post } from './post-types'

export const primerPosts1: Post[] = [
{
    slug: "building-reliable-data-products",
    title: "On Building Reliable Data Products",
    date: "2026-09-06",
    category: "primer",
    excerpt: "Reliable data products are more than pipelines that succeed — they need clear consumers, contracts, quality gates, and a rebuild path. A practical primer from a data analysis and bioinformatics lens.",
    html: "<p>Reliable data products are not just pipelines that finish without errors. They are artefacts that stakeholders can trust on Monday morning — tables, dashboards, feature stores, and analysis-ready datasets whose meaning, freshness, and lineage are clear. In bioinformatics and broader data engineering work, that trust is earned through deliberate design, not luck.</p>\n<p>This primer shares a practical framing I use when building data products: treat reliability as a product property, not a last-mile ops concern.</p>\n<h2>What a data product actually is</h2>\n<p>A data product has a defined consumer, a contract, and a lifecycle. The consumer might be a wet-lab collaborator reading a differential expression table, a downstream ML job, or a clinical operations dashboard. The contract covers schema, grain, update cadence, and known limitations. The lifecycle covers ownership, versioning, and retirement.</p>\n<p>If you cannot name the consumer and the contract in a few sentences, you probably have a pipeline — not yet a product.</p>\n<h2>Start with contracts, not code</h2>\n<p>Before writing transforms, write down:</p>\n<ul>\n<li><strong>Grain</strong> — one row per what? Sample? Gene–sample pair? Patient–visit?</li>\n<li><strong>Keys</strong> — which columns uniquely identify a row, and how do they join to other datasets?</li>\n<li><strong>Freshness</strong> — how stale can the data be before it is harmful?</li>\n<li><strong>Null behaviour</strong> — when is a missing value informative versus a bug?</li>\n<li><strong>Units and ontology</strong> — gene IDs, assay versions, timezones, currency of metadata.</li>\n</ul>\n<p>These decisions prevent the classic failure mode where two teams build slightly different \"truth\" tables and argue in meetings instead of analysing data.</p>\n<h2>Make quality checks first-class</h2>\n<p>Reliability means detecting broken assumptions early. Prefer small, readable checks over elaborate frameworks at the start:</p>\n<ul>\n<li>Row counts within expected bounds after each major step.</li>\n<li>Primary-key uniqueness and foreign-key coverage.</li>\n<li>Distribution checks on critical numeric fields (library size, QC metrics, yield).</li>\n<li>Schema drift detection when upstream files change column names or types.</li>\n</ul>\n<p>In practice, a handful of assertions in your orchestration layer (or a simple validation script) catches most silent corruption before it reaches a notebook.</p>\n<h2>Design for reprocessing</h2>\n<p>Experiments get re-run. Metadata gets corrected. A reliable product can be rebuilt from raw inputs without archaeology. That usually means:</p>\n<ol>\n<li>Immutable raw landing zones (never overwrite source extracts).</li>\n<li>Deterministic transforms with pinned package versions where scientific results matter.</li>\n<li>Clear separation of <em>raw</em>, <em>cleaned</em>, and <em>serving</em> layers.</li>\n<li>Run identifiers so you can answer \"which code produced this table?\".</li>\n</ol>\n<p>Reproducibility is not only a publication requirement — it is how you sleep when someone asks for last quarter's cohort definition again.</p>\n<h2>Observability without theatre</h2>\n<p>You do not need a wall of red–green panels on day one. You do need answers to three questions when something fails:</p>\n<ul>\n<li>What broke (step, table, check)?</li>\n<li>Who is affected (which consumers)?</li>\n<li>What is the recovery path (rerun, backfill, or temporary freeze)?</li>\n</ul>\n<p>Log structured run metadata. Alert on consumer-facing freshness and failed quality checks first; refine later.</p>\n<h2>Communicate limitations honestly</h2>\n<p>Every dataset has blind spots — batch effects, missing covariates, delayed labels. Document them next to the product, not in a private Slack thread. A short README with known issues often prevents more harm than another chart.</p>\n<h2>A simple checklist before you ship</h2>\n<ul>\n<li>Consumer and use case named.</li>\n<li>Schema and grain written down.</li>\n<li>At least one automated quality gate.</li>\n<li>Rebuild path tested once end-to-end.</li>\n<li>Owner and escalation path clear.</li>\n</ul>\n<p>Reliable data products grow from this discipline. The tooling will change; the product mindset will not. Build for the person who has to trust your table under time pressure — that is usually the best design constraint you can pick.</p>",
  },
{
    slug: "maintainable-python",
    title: "Tips for Writing Maintainable Python Code",
    date: "2026-09-06",
    category: "primer",
    excerpt: "Practical habits for readable, reusable Python in analysis and bioinformatics — project layout, pinned environments, small tests, and clarity over cleverness.",
    html: "<p>Maintainable Python is less about cleverness and more about kindness to your future self — and to whoever inherits your analysis next quarter. In data analysis and bioinformatics, scripts often start as exploratory notebooks and quietly become production. A few habits keep that transition from becoming painful.</p>\n<h2>Prefer clarity over cleverness</h2>\n<p>Readable code wins. Use descriptive names (<code>filtered_counts</code> over <code>df2</code>), keep functions short enough to skim, and avoid nesting that needs a map to parse. If a line needs a comment to explain <em>what</em> it does, rewrite the line. Reserve comments for <em>why</em> — the scientific rationale, the quirk in an upstream file, the reason a threshold exists.</p>\n<pre><code>def filter_low_counts(counts, min_cpm=1.0, min_samples=3):\n    \"\"\"Keep genes with CPM above threshold in enough samples.\"\"\"\n    cpm = counts.div(counts.sum(axis=0), axis=1) * 1e6\n    keep = (cpm &gt; min_cpm).sum(axis=1) &gt;= min_samples\n    return counts.loc[keep]</code></pre>\n<p>The docstring and names already carry most of the story.</p>\n<h2>Structure projects early</h2>\n<p>Even small analyses benefit from a predictable layout:</p>\n<ul>\n<li><code>data/raw</code> and <code>data/processed</code> (never overwrite raw).</li>\n<li><code>src/</code> for reusable functions.</li>\n<li><code>notebooks/</code> for exploration only.</li>\n<li><code>scripts/</code> or <code>pipelines/</code> for runnable entry points.</li>\n<li><code>tests/</code> for the bits that must not silently drift.</li>\n</ul>\n<p>Move logic out of notebooks into modules as soon as you find yourself copying cells between projects. Notebooks are wonderful for thinking; modules are better for repeating.</p>\n<h2>Dependencies are part of the science</h2>\n<p>Pin versions for anything that can change numerical results — <code>numpy</code>, <code>pandas</code>, Bioconductor bridges, plotting stacks. Record the environment with <code>requirements.txt</code>, <code>environment.yml</code>, or a lock file. In collaborative bioinformatics work, \"it worked on my machine\" is not a methodology section.</p>\n<pre><code>python -m venv .venv\nsource .venv/bin/activate\npip install -r requirements.txt</code></pre>\n<h2>Type hints and small interfaces</h2>\n<p>You do not need heavyweight typing everywhere. A few annotations on public functions make IDEs and reviewers more effective:</p>\n<pre><code>from pathlib import Path\nimport pandas as pd\n\ndef load_sample_sheet(path: Path) -&gt; pd.DataFrame:\n    return pd.read_csv(path, dtype={\"sample_id\": \"string\"})</code></pre>\n<p>Keep function signatures boring: explicit inputs, explicit outputs, minimal hidden global state.</p>\n<h2>Handle paths and configuration deliberately</h2>\n<p>Hard-coded absolute paths break the moment someone else clones the repo. Prefer project-relative paths, environment variables for secrets and machine-specific roots, and a single config module or YAML file for analysis parameters (thresholds, genome build, output directories).</p>\n<h2>Test the risky bits</h2>\n<p>You do not need 100% coverage. Test joins that drop rows, ID mapping, date parsing, and any custom statistic you would be embarrassed to get wrong. A tiny <code>pytest</code> suite on those functions pays for itself the first time a metadata column changes type.</p>\n<pre><code>def test_filter_low_counts_keeps_expressed_genes():\n    counts = make_toy_counts()\n    out = filter_low_counts(counts, min_cpm=1.0, min_samples=2)\n    assert \"highly_expressed_gene\" in out.index</code></pre>\n<h2>Style tools are cheap insurance</h2>\n<p>Use a formatter (<code>ruff format</code> or <code>black</code>), a linter (<code>ruff</code>), and consistent import ordering. Agree once as a team and stop debating whitespace. Consistency lowers cognitive load when reviewing scientific code.</p>\n<h2>A practical baseline</h2>\n<ol>\n<li>Name things for the domain, not the intermediate step number.</li>\n<li>Separate exploration from reusable logic.</li>\n<li>Pin dependencies that affect results.</li>\n<li>Test irreversible or subtle transforms.</li>\n<li>Document assumptions next to the code that encodes them.</li>\n</ol>\n<p>Maintainable Python is a habit, not a rewrite project. Apply these practices incrementally — each cleaned function and pinned environment makes the next analysis safer and faster.</p>",
  },
{
    slug: "common-unix-commands",
    title: "Common UNIX Commands",
    date: "2026-09-06",
    category: "primer",
    excerpt: "A compact UNIX toolkit for analysts and bioinformaticians — inspecting large files, composing pipes, compression, search, and everyday shell hygiene.",
    html: "<p>UNIX command-line tools remain the fastest way to inspect files, glue workflows together, and survive large bioinformatics datasets without opening a GUI. You do not need to memorise everything — a compact toolkit covers most day-to-day work for analysts and data engineers.</p>\n<p>This primer focuses on commands I reach for constantly when exploring sequencing outputs, logs, and tabular intermediates.</p>\n<h2>Moving around and seeing what is there</h2>\n<pre><code>pwd\nls -lah\ncd /path/to/project\ntree -L 2   # if installed</code></pre>\n<p><code>ls -lah</code> shows hidden files, human-readable sizes, and permissions. Permissions matter when shared cluster directories refuse to write.</p>\n<h2>Peeking at files safely</h2>\n<p>Never open a multi-gigabyte FASTQ in a graphical editor. Use streaming viewers:</p>\n<pre><code>head -n 20 sample.fastq\ntail -n 50 run.log\nless big_matrix.tsv\nwc -l counts.csv</code></pre>\n<p><code>less</code> lets you search with <code>/</code> and quit with <code>q</code>. <code>wc -l</code> is a quick sanity check after filters.</p>\n<h2>Finding things</h2>\n<pre><code>find data/raw -name \"*.fastq.gz\"\ngrep -n \"ERROR\" logs/pipeline.log\nrg \"sample_id\" metadata/   # ripgrep, if available</code></pre>\n<p><code>find</code> locates files by name or pattern; <code>grep</code>/<code>rg</code> locate text inside files. Prefer ripgrep on large codebases — it is fast and respects ignore rules.</p>\n<h2>Compression and archives</h2>\n<p>Omics data lives compressed. Essential patterns:</p>\n<pre><code>gzip file.txt\ngunzip file.txt.gz\nzcat sample.fastq.gz | head\ntar -xzf archive.tar.gz\ntar -czf backup.tar.gz results/</code></pre>\n<p><code>zcat</code> (or <code>gzip -dc</code>) streams compressed text without writing an uncompressed copy to disk — invaluable on clusters with tight quotas.</p>\n<h2>Pipes, redirects, and composition</h2>\n<p>The UNIX philosophy shines when you chain small tools:</p>\n<pre><code>zcat reads.fastq.gz | head -n 400 | grep \"^@\" | wc -l\ncut -f1,3 counts.tsv | sort | uniq -c | sort -nr | head</code></pre>\n<p>Stdout of one command feeds stdin of the next. Redirect with <code>&gt;</code> (overwrite) or <code>&gt;&gt;</code> (append); send errors aside with <code>2&gt;</code> when needed.</p>\n<h2>Columns and quick tabular surgery</h2>\n<pre><code>cut -d',' -f1,4 samples.csv\nawk -F'\\t' 'NR==1 || $3 &gt; 10' genes.tsv\nsort -k2,2nr results.tsv | head</code></pre>\n<p><code>cut</code> selects fields; <code>awk</code> filters and transforms; <code>sort</code> orders. For serious tabular work you will still use Python or R — but these one-liners are perfect for smoke tests.</p>\n<h2>Permissions, processes, and disk</h2>\n<pre><code>chmod u+x run.sh\ndf -h\ndu -sh data/*\ntop\nhtop   # if installed\nkill PID</code></pre>\n<p>Know when a job is CPU-bound versus waiting on disk. <code>du</code> finds which directory ate the quota before someone else's job fails.</p>\n<h2>Remote and transfer basics</h2>\n<pre><code>ssh user@cluster\nscp local.file user@cluster:/path/\nrsync -avP data/ user@cluster:/path/data/</code></pre>\n<p><code>rsync</code> is usually kinder than <code>scp</code> for large directories — it resumes and shows progress.</p>\n<h2>Shell hygiene that saves weekends</h2>\n<ul>\n<li>Quote variables: <code>\"$file\"</code> — spaces in filenames are common.</li>\n<li>Stop on failure in scripts: <code>set -euo pipefail</code>.</li>\n<li>Prefer absolute paths in scheduled jobs.</li>\n<li>Keep a short personal cheatsheet of the ten commands you actually use.</li>\n</ul>\n<h2>Closing thought</h2>\n<p>UNIX fluency is a force multiplier for data work. Learn the small set above deeply — composition, compression, inspection, and search — and you will spend less time fighting files and more time analysing them. Everything else can be looked up as needed.</p>",
  },
{
    slug: "git-essentials",
    title: "Git Essentials",
    date: "2026-09-06",
    category: "primer",
    excerpt: "The Git daily loop for scientific and data work — status, diff, commit, branch, and safe undo — without drowning in advanced history surgery.",
    html: "<p>Git is the shared memory of a software or analysis project. For data analysts and bioinformaticians, it is how you recover yesterday's working script, review a colleague's change to a pipeline, and explain exactly which code produced a figure. You do not need every advanced command — a solid essentials kit covers most real work.</p>\n<h2>Why Git matters in scientific data work</h2>\n<p>Analyses evolve. Parameters change. Reviewers ask for a tweak six months later. Git gives you a chronological story of those decisions. Pair it with clear commits and you can reconstruct not only <em>what</em> changed, but often <em>why</em>.</p>\n<p>Keep large raw data out of Git. Version the code, configs, and small metadata; store bulky omics files in appropriate data stores and record accessions or checksums in the repo.</p>\n<h2>The daily loop</h2>\n<pre><code>git status\ngit diff\ngit add path/to/file\ngit commit -m \"Describe why this change exists\"\ngit push</code></pre>\n<p><code>status</code> and <code>diff</code> are your habits before every commit. Read the diff. Accidental notebook outputs and credentials show up there more often than anyone likes to admit.</p>\n<h2>Clone, branch, merge</h2>\n<pre><code>git clone git@example.com:org/project.git\ngit checkout -b feature/qc-filters\n# ... edit ...\ngit add -p\ngit commit -m \"Tighten CPM filter for low-depth libraries\"\ngit checkout main\ngit pull\ngit merge feature/qc-filters</code></pre>\n<p>Branches let you try ideas without breaking <code>main</code>. Name them for intent (<code>fix/sample-sheet-join</code>, <code>docs/readme-setup</code>). Prefer small branches that are easy to review.</p>\n<h2>Write commits for humans</h2>\n<p>A good commit message completes the sentence \"This commit will...\". Focus on motivation:</p>\n<ul>\n<li>Good: <em>Exclude control wells from normalisation cohort</em></li>\n<li>Weak: <em>Update script</em></li>\n</ul>\n<p>Atomic commits — one logical change each — make reverts and archaeology easier.</p>\n<h2>Inspect history without fear</h2>\n<pre><code>git log --oneline -n 20\ngit log -p -- path/to/file\ngit blame path/to/file\ngit show COMMIT</code></pre>\n<p><code>blame</code> is not about blame — it answers \"when did this line arrive?\". Combined with <code>log -p</code>, it is the fastest way to understand surprising behaviour in a shared pipeline.</p>\n<h2>Undo safely</h2>\n<p>Common, relatively safe moves:</p>\n<pre><code>git restore file.py          # discard unstaged edits to a file\ngit restore --staged file.py # unstage\ngit revert COMMIT            # add a new commit that undoes a past one\ngit reset --soft HEAD~1      # undo last commit, keep changes staged</code></pre>\n<p>Avoid rewriting published history on shared branches (<code>reset --hard</code>, force-push) unless the team explicitly agrees. Prefer <code>revert</code> for changes already on <code>main</code>.</p>\n<h2>.gitignore for analysis repos</h2>\n<p>Ignore the noise: virtualenvs, <code>__pycache__</code>, large intermediates, local secrets, and notebook checkpoint folders. Commit an example env file (<code>.env.example</code>) instead of real credentials. Never commit API keys or private key files — rotate immediately if you slip.</p>\n<pre><code>.venv/\n__pycache__/\ndata/raw/\n*.h5ad\n.env\n.ipynb_checkpoints/</code></pre>\n<h2>Collaboration basics</h2>\n<ul>\n<li>Pull before you push.</li>\n<li>Use pull requests for anything non-trivial — review is part of quality control.</li>\n<li>Resolve conflicts calmly: understand both sides, do not simply accept theirs or yours blindly.</li>\n<li>Tag releases when a pipeline version underpins a paper or production dashboard (<code>v1.2.0</code>).</li>\n</ul>\n<h2>A minimal mental model</h2>\n<ol>\n<li>Working tree — your files now.</li>\n<li>Staging area — what the next commit will include.</li>\n<li>Repository history — commits on branches.</li>\n<li>Remote — the shared copy on the server.</li>\n</ol>\n<p>Master moving changes deliberately between those layers and Git stops feeling mystical. Essentials first; rebases and advanced history surgery can wait until the daily loop is second nature.</p>",
  },
{
    slug: "software-engineering-for-bioinformatics",
    title: "Software Engineering Concepts Every Bioinformatics Professional Should Know",
    date: "2026-09-06",
    category: "primer",
    excerpt: "Modularity, reproducibility, testing, configuration, and workflows — the software engineering ideas that keep bioinformatics analyses trustworthy and reusable.",
    html: "<p>Bioinformatics sits at the intersection of biology and software. The biology may be your passion, but the software engineering habits you bring to scripts, pipelines, and shared tools largely decide whether results are trustworthy and reusable. You do not need to become a full-time software engineer — you do need a few durable concepts.</p>\n<h2>Modularity and separation of concerns</h2>\n<p>Split work into stages with clear inputs and outputs: ingest, QC, transform, analyse, report. Each stage should be runnable on its own with documented artefacts. When QC is tangled inside differential expression code, every change risks breaking both.</p>\n<p>In practice, modularity looks like small functions or workflow steps, stable intermediate files or tables, and interfaces that do not leak temporary assumptions.</p>\n<h2>Reproducibility as a design goal</h2>\n<p>A result you cannot regenerate is an anecdote. Aim for:</p>\n<ul>\n<li>Pinned software environments (Conda, containers, or lock files).</li>\n<li>Recorded parameters and random seeds where stochastic methods appear.</li>\n<li>Referenced data versions (GEO accessions, genome builds, container digests).</li>\n<li>Runnable entry points — a script or workflow a colleague can launch without a guided tour.</li>\n</ul>\n<pre><code># Example: record the stack beside results\npython -m pip freeze &gt; results/run_2026-09-06/requirements.lock</code></pre>\n<p>Containers (Docker/Singularity) are especially helpful on shared HPCs where \"module load\" combinations drift over time.</p>\n<h2>Testing is not optional for critical paths</h2>\n<p>Scientific code fails quietly — joins drop samples, gene ID maps collide, strandedness flips. Add automated checks where mistakes are costly:</p>\n<ul>\n<li>Unit tests for ID mapping and filter logic.</li>\n<li>Smoke tests that run a tiny fixture dataset through the pipeline.</li>\n<li>Schema checks on outputs (columns, types, allowed values).</li>\n</ul>\n<p>Even a handful of tests around sample-sheet parsing will save days over a project's life.</p>\n<h2>Version control and code review</h2>\n<p>Use Git for code and configuration. Review changes that affect analysis logic the same way you would review a methods paragraph — because they <em>are</em> methods. Pull requests create a paper trail of design decisions that notebooks alone rarely capture.</p>\n<h2>Configuration over hard-coding</h2>\n<p>Thresholds, reference paths, and cohort definitions change. Put them in config files or explicit parameters rather than burying magic numbers in deep functions. Your future self will need to rerun sensitivity analyses; make that a one-line edit, not a scavenger hunt.</p>\n<pre><code># config.yaml\nmin_cpm: 1.0\nmin_samples: 3\ngenome_build: GRCh38\nstrandedness: reverse</code></pre>\n<h2>Logging, errors, and observability</h2>\n<p>Fail loudly with useful messages. Capture run IDs, input hashes, and key counts at each stage. When a collaborator says \"the heatmap looks wrong\", logs that show how many genes survived filtering are worth more than a vague stack trace.</p>\n<h2>Data contracts and metadata discipline</h2>\n<p>Agree on sample identifiers, ontology for conditions, and units. Treat metadata as part of the product: inconsistent labels cause more incorrect biology papers than exotic statistical choices. Validate early that every assay file has a matching metadata row.</p>\n<h2>Performance: measure before you rewrite</h2>\n<p>Bioinformatics datasets can be large, but premature optimisation wastes time. Profile when something is slow. Prefer efficient formats (Parquet, indexed BAM/CRAM) and vectorised operations before rewriting algorithms. Parallelise embarrassingly parallel sample-level steps with workflow managers rather than ad-hoc background jobs.</p>\n<h2>Workflow managers</h2>\n<p>Tools like Snakemake, Nextflow, or CWL encode dependencies between steps so rebuilds are incremental and transparent. They also make cluster submission less fragile than a nest of shell scripts. Learn one well enough to express a linear pipeline cleanly — that skill transfers.</p>\n<h2>Ethics and stewardship</h2>\n<p>Access controls, de-identification, and data retention are software concerns too. Keep secrets out of repos. Know which datasets are shareable under which licences. Engineering discipline includes protecting participants and collaborators, not only shipping plots.</p>\n<h2>Start where you are</h2>\n<ol>\n<li>Put analysis code in Git with a clear README.</li>\n<li>Pin the environment for any result you might publish or operationalise.</li>\n<li>Add tests around the transforms you fear.</li>\n<li>Externalise parameters.</li>\n<li>Automate the happy path with a workflow or scripted entry point.</li>\n</ol>\n<p>Software engineering concepts are amplifiers for biological insight. Adopt them gradually, with empathy for scientific iteration — and your pipelines will stay trustworthy as questions get harder.</p>",
  }
]
